package com.example.enums;

/** Token and customer-response lifecycle for one investigation outreach message. */
public enum InvestigationResponseStatus {
    AWAITING_RESPONSE,
    RESPONDED,
    EXPIRED
}